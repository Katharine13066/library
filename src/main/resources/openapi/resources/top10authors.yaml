get:
  tags:
    - bookhistory
  summary: Get top 10 authors
  operationId: get10TheMostPopularAuthors
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/Author.yaml'