get:
  tags:
    - authors
  summary: Get author by id
  operationId: findByIdAuthor
  parameters:
    - $ref: '../parameters/id.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../schemas/Author.yaml'