get:
  tags:
    - BookHistory Controller
  summary: Get bookHistory by id
  operationId: findByIdBookHistory
  parameters:
    - $ref:  '../parameters/id.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../schemas/BookHistoryDTO.yaml'